import { DateChangeCancelModel } from './date-change-cancel-model.model';

describe('DateChangeCancelModel', () => {
  it('should create an instance', () => {
    expect(new DateChangeCancelModel()).toBeTruthy();
  });
});
