import { PassengerInfoModel } from './passenger-info-model';

describe('PassengerInfoModel', () => {
  it('should create an instance', () => {
    expect(new PassengerInfoModel()).toBeTruthy();
  });
});
