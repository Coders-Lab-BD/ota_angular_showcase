import { MarkuDiscountModel } from './marku-discount-model.model';

describe('MarkuDiscountModel', () => {
  it('should create an instance', () => {
    expect(new MarkuDiscountModel()).toBeTruthy();
  });
});
