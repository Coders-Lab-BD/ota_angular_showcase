import { AppClickOutsideDirective } from './app-click-outside.directive';

describe('AppClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new AppClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
